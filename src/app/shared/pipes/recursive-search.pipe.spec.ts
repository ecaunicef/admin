import { RecursiveSearchPipe } from './recursive-search.pipe';

describe('RecursiveSearchPipe', () => {
  it('create an instance', () => {
    const pipe = new RecursiveSearchPipe();
    expect(pipe).toBeTruthy();
  });
});
