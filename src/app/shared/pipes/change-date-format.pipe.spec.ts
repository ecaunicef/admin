import { ChangeDateFormatPipe } from './change-date-format.pipe';

describe('ChangeDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new ChangeDateFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
